<div class="columns">
  <div class="column">
    <h2 class="title is-4">Top Movies<h2>
    <ol class="ml-5">
      <% @movies.each do |movie| %>
        <li>
          <%= link_to movie.title, movie %>
          <em>(<%= movie.production_company.name %>)</em>
        </li>
      <% end %>
    </ol>
    <p class="mt-4">
      <strong>
        <%= link_to '👉 See All 100 Top Movies', movies_path %>
      </strong>
    </p>
  </div>

  <div class="column">
    <h2 class="title is-4">Top Production Companies<h2>
    <ol class="ml-5">
      <% @production_companies.each do |company| %>
        <li>
          <%= link_to company.name, company %>
        </li>
      <% end %>
    </ol>
    <p class="mt-4">
      <strong>
        <%= link_to '👉 See All Top Production Companies', production_companies_path %>
      </strong>
    </p>
  </div>
</div>